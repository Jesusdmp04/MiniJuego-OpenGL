#include "Texto.h"

void Texto::init(Shader &shader) {
    // setup para render de texto (p. ej. cargar atlas de font)
}

void Texto::render(const std::string &text, float x, float y, float scale) {
    // dibujar cada carácter con quads y textura
}
